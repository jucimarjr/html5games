/* The possible states in the finite state machine */

var States = Object.freeze({
    FIRST: "States.FIRST",
    LOGIN: "States.LOGIN",
    ROOMS: "States.ROOMS",
    WAIT_MORE_PLAYERS: "States.WAIT_MORE_PLAYERS"
});